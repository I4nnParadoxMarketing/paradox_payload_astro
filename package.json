{
  "name": "paradox-payload-astro",
  "private": true,
  "workspaces": [
    "payload",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=payload & npm run dev --workspace=frontend",
    "dev:payload": "npm run dev --workspace=payload",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=payload && npm run build --workspace=frontend",
    "seed": "npm run seed --workspace=paradox-payload",
    "seed:menu": "npm run seed:menu --workspace=paradox-payload",
    "import:wordpress": "npm run import:wordpress --workspace=paradox-payload",
    "import:capabilities": "npm run import:capabilities --workspace=paradox-payload"
  },
  "dependencies": {
    "@neon/config": "^1.3.2",
    "@neon/env": "^1.2.3"
  }
}
